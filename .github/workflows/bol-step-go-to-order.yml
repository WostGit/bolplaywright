name: "Bol - Step 3: Go to Order / Checkout"

on:
  workflow_call:
    inputs:
      bol-email:
        required: true
        type: string

jobs:
  go-to-order:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PROXY_SERVER: ${{ secrets.PROXY_SERVER }}
      PROXY_USERNAME: ${{ secrets.PROXY_USERNAME }}
      PROXY_PASSWORD: ${{ secrets.PROXY_PASSWORD }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Execute Go to Order Step
        shell: bash
        run: |
          set -euo pipefail
          echo "================================================================="
          echo " STEP 3: GO TO ORDER / CHECKOUT"
          echo "================================================================="

          cat > bol-go-order.spec.js <<'EOF'
          const { test, chromium } = require('@playwright/test');
          const fs = require('fs');
          const path = require('path');

          test('Proceed to Checkout', async () => {
            const botToken = process.env.TELEGRAM_BOT_TOKEN;
            const chatId = process.env.TELEGRAM_CHAT_ID;

            async function sendTelegramAlert(filePath, caption) {
              if (!botToken || !chatId) return;
              try {
                const { request } = require('@playwright/test');
                const apiContext = await request.newContext();
                const buffer = fs.readFileSync(filePath);
                await apiContext.post(`https://api.telegram.org/bot${botToken}/sendPhoto`, {
                  multipart: {
                    chat_id: chatId,
                    caption: caption,
                    photo: {
                      name: path.basename(filePath),
                      mimeType: 'image/png',
                      buffer: buffer
                    }
                  }
                });
              } catch (e) {
                console.error(e.message);
              }
            }

            const browser = await chromium.launch({ headless: false });
            const context = await browser.newContext({
              proxy: {
                server: process.env.PROXY_SERVER,
                username: process.env.PROXY_USERNAME,
                password: process.env.PROXY_PASSWORD
              }
            });
            const page = await context.newPage();

            try {
              await page.goto('https://www.bol.com/nl/nl/winkelwagentje/');

              // Click the checkout button
              await page.getByRole('link', { name: /Naar bestellen|Bestellen|Doorgaan/i }).first().click({ force: true });

              await page.waitForLoadState('networkidle');

              const screenshotPath = 'checkout-page.png';
              await page.screenshot({ path: screenshotPath, fullPage: true });
              await sendTelegramAlert(screenshotPath, '✅ Reached Checkout / Order page');

              console.log('Navigated to order page successfully');
            } finally {
              await context.close();
              await browser.close();
            }
          });
          EOF

          xvfb-run npx playwright test bol-go-order.spec.js
